akka {
  stdout-loglevel = "ERROR"
  loglevel = "ERROR"
  log-dead-letters = 10
  log-dead-letters-during-shutdown = on
  actor {
    debug {
      receive = on
      autoreceive = on
      lifecycle = off
      unhandled = on
    }
  }
}
